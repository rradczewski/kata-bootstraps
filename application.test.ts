describe("A Test", () => {
  it("fails", () => {
    expect(false as boolean).toEqual(true as boolean);
  });

    it("succeeds", () => {
    expect(true as boolean).toEqual(true as boolean);
  });

});
